VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-public-key"

# Required for admin to create driver Auth accounts from the dashboard.
# Find this in: Supabase Dashboard > Project Settings > API > service_role key
# ⚠️ Pilot-test only — never expose in public/production deployments.
VITE_SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

